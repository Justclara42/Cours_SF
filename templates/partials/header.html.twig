<header>
    <nav class="bg-red-800 p-4 border-b-2 border-yellow-500 text-white">
        <div class="container mx-auto flex justify-between items-center">
            <a href="{{ path('app_home_index') }}" class="flex items-center text-white text-lg font-bold">
                <img src="{{ asset('images/logo.png') }}" class="w-12" alt="logo">
                Tagada CRM
            </a>
            <ul class="flex space-x-4">
                <li><a href="{{ path('app_home_index') }}" class="text-gray-300 hover:text-white">Home</a></li>
                <li><a href="{{ path('app_user_index') }}" class="text-gray-300 hover:text-white">Users</a></li>
                <li><a href="{{ path('app_client') }}" class="text-gray-300 hover:text-white">Clients</a></li>
                <li><a href="{{ path('app_user_login') }}" class="text-gray-300 hover:text-white">Login</a></li>
                <li><a href="{{ path('app_user_register') }}" class="text-gray-300 hover:text-white">Register</a></li>
                <li><a href="{{ path('app_home_index') }}" class="text-gray-300 hover:text-white">Contact</a></li>
            </ul>
            <form action="{{ path('app_client_search') }}" class="flex items-center space-x-2">
                <input type="text" name="search" placeholder="Search clients..." class="px-4 py-2 rounded-md bg-white text-gray-800 focus:outline-none focus:ring-2 focus:ring-yellow-500" required>
                <button type="submit" class="bg-yellow-500 hover:bg-yellow-600 text-white px-4 py-2 rounded-md transition-colors duration-200">Search</button>
            </form>
        </div>
    </nav>
</header>
